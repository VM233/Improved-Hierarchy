# Hierarchy query allocation

MarbleBattlers capture `83375ce9c8b34a89b3a4ce2695005bbe`, frame 4229, attributes 85.226 ms and 1.0 MB to `NewHierarchyWindowController.RepaintAll`. Frame 4201 attributes 7.388 ms and 151.5 KB to its periodic scan. The frozen Hierarchy window contains 1,551 visual nodes, 35 visible rows, maximum depth 18, 21 component-icon roots and 15 main-icon hosts. The recursive `FindAll` iterator creates an enumerator for every visited node, including unmatched nodes. Icon cleanup repeats that allocation for each class query.

The 1.0.1 repair kept row discovery and invalidation in the window controller, and generated icon removal and host tint restoration in the icon renderer. The replacement queries use the same exact row name and icon classes through UI Toolkit, materializing each result list before any tree mutation. Each query returns its current tree snapshot. The obsolete recursive all-match utility is removed. The subsequent 1.0.2 row-lifetime repair is described in `hierarchy-row-lifecycle.md`.

Static Cost Ledger before executable writes: one frozen window, 1,551 nodes, 35 disjoint row subtrees and depth at most 18. A row discovery visits each node once. Each icon cleanup performs three linear queries on its row subtree. The existing two cleanup calls in some row redraw paths remain within at most six row-subtree traversals, so a complete refresh visits at most 10,857 nodes across discovery and cleanup, with at most 211 query/result objects. There is no query per scene object and no new traversal. Result lists retain only matches, at most 1,551 references across disjoint rows plus 35 row references. Query construction has constant managed state per query instead of one iterator per traversed node. The frozen query scratch and result reference allowance is 256 KiB, and all traversals remain on the Editor main thread. PASS for removing the allocation multiplier. Rendering, object reflection and icon construction retain their existing separate costs and require the repeat capture.

Focused validation covers exact-class cleanup, preservation of unrelated elements, root/nested matches, tint restoration, repeated cleanup and allocation growth when the same empty-match cleanup grows from 32 to 1,551 nodes. Test construction is outside the allocation window, with at most 1,583 retained detached visual elements and twenty measured cleanup calls, at most 47,490 node visits. Main-project validation compares the real Hierarchy tree and repeats the same bounded calibration capture. A successful compile alone does not establish the performance result.

The query API is available in the package's Unity 6000.0 support floor: [UQueryBuilder.ToList](https://docs.unity3d.com/6000.0/Documentation/ScriptReference/UIElements.UQueryBuilder_1.ToList.html). UI Toolkit's reference implementation traverses the tree into a result list without creating one iterator per node. The native query remains the search owner.
